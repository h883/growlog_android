{
  "name": "growlog-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply growlog-db --local",
    "db:migrate": "wrangler d1 migrations apply growlog-db"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.99.0"
  }
}

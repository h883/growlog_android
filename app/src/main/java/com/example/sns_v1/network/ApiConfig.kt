package com.example.sns_v1.network

object ApiConfig {
    // Replace with your actual Workers URL after deployment
    // e.g. "https://growlog-api.YOUR_SUBDOMAIN.workers.dev"
    const val BASE_URL = "https://growlog-api.nopzqql.workers.dev"

    /** 画像取得時にトークンを付ける相手を限定するために使う */
    const val HOST = "growlog-api.nopzqql.workers.dev"
}
